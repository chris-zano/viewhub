<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
</head>
<body>

    <% if (pageTitle == "signup") { %>
        <%- include('./layouts/signup.ejs') %>
    <% } %>
    <% if (pageTitle == "login") { %>
        <%- include('./layouts/login') %>
    <% } %>
    <% if (pageTitle == "Create-Profile ~ name") { %>
        <%- include('./layouts/flname') %>
    <% } %>
    <% if (pageTitle == "Create-Profile ~ gender") { %>
        <%- include('./layouts/gender') %>
    <% } %>
    <% if (pageTitle == "Create-Profile ~ username") { %>
        <%- include('./layouts/username') %>
    <% } %>
    <% if (pageTitle == "Create-Profile ~ picture") { %>
        <%- include('./layouts/picture') %>
    <% } %>
</body>
</html>